class Post {
    constructor(titleText, bodyText, pub, featured, tag) {
		//this.id = id;
        this.title = titleText;
        this.body = bodyText;
        this.public = pub;
        this.featured = featured;
        this.tag = tag;
    }
}